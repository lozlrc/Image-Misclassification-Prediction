# Image-Misclassification-Prediction
Build a CIFAR-10 image classifier, then train a second model that predicts when the classifier will be wrong. Use this “failure risk” to abstain on uncertain images and improve reliability with clear evaluation plots and a Streamlit demo
